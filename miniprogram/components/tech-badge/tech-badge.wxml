<view class="tech-badge {{size}}" style="background: {{color}}15; border-color: {{color}}30;" bindtap="onTap">
  <image wx:if="{{icon}}" class="badge-icon" src="{{icon}}" mode="aspectFit"/>
  <text class="badge-name" style="color: {{color}};">{{name}}</text>
</view>
